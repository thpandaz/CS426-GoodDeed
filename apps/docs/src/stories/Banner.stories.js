import { Banner } from "@repo/ui-web"

export default {
    title: "Components/Banner",
    component: Banner,
    parameters: {
        layout: 'fullscreen'
    }
}

export const Primary = {
    args: {
        tagline: 'Connecting Hearts, Changing Lives'
    }
}